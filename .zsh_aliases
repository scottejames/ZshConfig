# Some useful aliases.
alias tt="tt++ ~/.config.tin"
alias ls='ls --color'
alias texclean='rm -f *.toc *.aux *.log *.cp *.fn *.tp *.vr *.pg *.ky'
alias clean='echo -n "Really clean this directory?";
	read yorn;
	if test "$yorn" = "y"; then
	   rm -f \#* *~ .*~ *.bak .*.bak  *.tmp .*.tmp core a.out;
	   echo "Cleaned.";
	else
	   echo "Not cleaned.";
	fi'
alias h='history'
alias j="jobs -l"
alias l="ls -l "
alias ll="ls -l"
alias pu="pushd"
alias po="popd"
alias xterm="xterm -bg black -fg gray"
alias xb="xterm -bg black -fg gray -e /home/sjames5/scripts/start-shell.sh"

alias fixmod='xmodmap -e "remove  mod1 = Mode_switch"'


alias dbsimparm="sqsh -S NYT_FIDCRED -U sybdcs -P sybdcs -Dmodeldb"
alias dbriskdata="sqsh -S NYT_DCSRISK -U sybdcs -P sybdcs -Ddcsrisk"
